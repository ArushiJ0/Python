# Sets

- Unordered
- Only unique elements
- It has properties like queue

### Membership Test -

to check if element is present in a set 
Ex - 3 in setName
It gives True or False

### Key Takeaways -

- If remove method is used and the element is not present it shows an error. If discard method is used it does not error.
- Frozenset can be used to make the sets immutable(no changes can be made) and also to use them in dictionaries.

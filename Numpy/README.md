# Numpy

np.array () creates an array

np.array([[elements]]) is used for 2D array 

to access elements in 2D array : arr[ rows , columns] Ex . arr[1: ,0:1]

mp.newaxis is used to change the shape of array

The library contains Linear Algebra functions (linalg) for inverse , determinants , eigenvalues

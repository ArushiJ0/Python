# Dictionaries

- Unordered
- Key value pairs
- Keys should be unique
- Dictionary is mutable

### Dictionary Methods -

- .get() - used to access the elements in the dictionary
- del is used to remove a key
- 
### JSON File

json.dumps() creates a python string where as dump creates a JSON file for reverse to convert json string/file to python it uses json.loads() which creates python string and load creates a python file

### Key Takeaway -

- Shallow copy .copy() is used to make a copy, this allocates a new memory to the copy so the changes made are not reflected in both
- To merge dictionaries ** is used
- default dictionary is imported from collections and it is used when key is not allotted so it doesn’t throw any error creating a default dictionary
